package CollectionsFramework;

import java.util.ArrayList;

public class Collections {

    public static void sort(ArrayList<Integer> list1) {
    }
    // the tools are already there at your dispoal -> collections of classes and interfaces
    // Collections Interface implements the iterable interface 
    // 1) List 2) Quene 3) Set -> are all data structures 

    // how do we use these data structures -> some classes inherit these and we use those to make classes 
    // add, size, remove, iterable, allAll, removeAll, clear - functions are pretty self explanatory

    // List interface -> Classes -> Arraylist, Linkedlist, Vector class -> stack 

    // Priority Quene - First in First out -> Quene -> Priority Quene, LinkedList, Deque(Double Ended Quene) -> ArrayDeque (Create objects of ArrayDeque

    // Set -> HashSet, LinkedHashSet, SortedSet -> TreeSet 
    // Set is a group of objects where every value is unique

    // Map Interface -> HashMap, LinkedHashMap, SortedMap -> TreeMap(Sorted according to the keys in ascending order usually), Hashtable
}
